import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // =========================
  // Backgrounds
  // =========================

  static const Color lightBackground =
      Color(0xFFF6F4F1);

  static const Color darkBackground =
      Color(0xFF171717);

  // =========================
  // Dashboard Cards
  // =========================

  // Wallet (Muted Sage)
  static const Color wallet =
      Color(0xFF66705D);

  // Goal (Muted Slate Blue)
  static const Color goal =
      Color(0xFF5D6670);

  // AI Insight (Warm Taupe)
  static const Color insight =
      Color(0xFF6F6A63);

  // Monthly Snapshot (Soft Graphite)
  static const Color snapshot =
      Color(0xFF707070);

  // Streak (Muted Bronze)
  static const Color streak =
      Color(0xFF736656);

  // =========================
  // Text
  // =========================

  static const Color textPrimary =
      Colors.white;

  static const Color textSecondary =
      Colors.white70;

  // =========================
  // Borders / Glass Effects
  // =========================

  static const Color glassBorder =
      Color(0x26FFFFFF);

  static const Color glassOverlay =
      Color(0x1AFFFFFF);
}